export interface FeaturesProps {
  imageSide?: "left" | "right";
}

export interface DottedBoxProps {
  //preparation in case, not useing top/bottom just yet
  position?: "left" | "right" | "top" | "bottom";
}
